<!DOCTYPE safe>
<safe>
<!-- Define the service names, in the startup order, separated by commas (,) -->
<macro name="SERVICES" value="postgresql"/>

<service mode="mirror" boot="on">
  <!-- Heartbeat Configuration -->
  <heart>
    <heartbeat name="default">
    </heartbeat>
  </heart>
  <!-- Virtual IP Configuration -->
  <!-- Define the name or IP address of your virtual server 
  -->
  <vip>
    <interface_list>
        <interface> 
	        <real_interface>
               <virtual_addr addr="" where="one_side_alias" />
          </real_interface>
        </interface>
    </interface_list>
  </vip>
  <!-- Software Error Detection Configuration -->
  <errd polltimer="10">
    <!-- PostgreSQL Server -->
    <proc name="postgresql" service="yes" action="restart" class="prim" />
  </errd>
  <!-- File Replication Configuration -->
  <!-- Replicate
     * /var/lib/pgsql/data: default directory path of PostgreSQL database and redo log
  -->
  <rfs>
	<replicated dir="/var/lib/pgsql/data" >
	  <notreplicated path="pg_stat_tmp"/>
	</replicated>
  </rfs>
  <!-- User scripts activation -->
   <user>
    <var name="SERVICES" value="%SERVICES%"/>
   </user>
</service>
</safe>
