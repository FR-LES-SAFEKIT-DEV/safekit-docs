<!DOCTYPE safe>
<safe>
<!-- Define the service names, in the startup order, separated by commas (,)
      Use the Get-Service PowerShell cmdlet to get the service names
  -->
<macro name="SERVICES" value="Firebird Server - DefaultInstance"/>
<service mode="mirror" boot="on">
  <!-- Heartbeat Configuration -->
  <heart>
    <heartbeat name="default">
    </heartbeat>
  </heart>
  <!-- Virtual IP Configuration -->
  <vip>
    <interface_list>
        <interface> 
	  <real_interface>
               <virtual_addr addr="" where="one_side_alias" />
          </real_interface>
        </interface>
    </interface_list>
  </vip>
  <!-- Software Error Detection Configuration -->
  <errd polltimer="10">
    <!-- Firebird Server -->
    <proc name="firebird.exe" action="restart" class="prim" />
  </errd>
  <!-- File Replication Configuration -->
  <!-- 
     * C:\Program Files\Firebird\Firebird_3_0\examples\empbuild: directory of employee.fdb sample database
     * replace the path with your database directory
  -->
  <rfs>
	  <replicated dir="C:\Program Files\Firebird\Firebird_3_0\examples\empbuild" />
  </rfs>
  <!-- User scripts activation -->
  <user>
    <var name="SERVICES" value="%SERVICES%"/>
  </user>
</service>
</safe>
